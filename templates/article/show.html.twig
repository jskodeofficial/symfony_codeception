{% extends 'base.html.twig' %}

{% block title %}Article{% endblock %}

{% block body %}
    <h1>Article</h1>

    <table class="table table-dark">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ article.id }}</td>
            </tr>
            <tr>
                <th>Date_publication</th>
                <td>{{ article.datePublication ? article.datePublication|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>title</th>
                <td>{{ article.title }}</td>
            </tr>
            <tr>
                <th>content</th>
                <td>{{ article.content }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_article_index') }}" class="btn btn-primary">Back to the list</a>

    <a href="{{ path('app_article_edit', {'id': article.id}) }}" class="btn btn-success">Edit</a>

    {{ include('article/_delete_form.html.twig') }}
{% endblock %}
